{
  "name": "svgedit-diagram-server",
  "version": "1.0.0",
  "description": "SVG diagram storage backend with HTTP API + WebSocket relay for SVGEdit + MCP integration",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "mcp": "node mcp-server.js"
  },

  "dependencies": {
    "ws": "^8.16.0",
    "uuid": "^11.1.0",
    "@modelcontextprotocol/sdk": "^1.12.1"
  }
}
